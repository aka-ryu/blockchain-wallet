import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    title: {
        fontSize: 40,
        fontWeight: 'bold'
    },
    titleLayer: {
        marginTop: 100,
        alignItems: 'center',
    }
});